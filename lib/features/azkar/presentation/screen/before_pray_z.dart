import 'package:flutter/material.dart';

class BeforePrayZ extends StatelessWidget {
  const BeforePrayZ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Before Pray Azkar'),
      ),
    );
  }
}
