import 'package:flutter/material.dart';

class AfterPrayZ extends StatelessWidget {
  const AfterPrayZ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('After Pray Azkar'),
      ),
    );
  }
}
