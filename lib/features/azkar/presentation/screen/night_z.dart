import 'package:flutter/material.dart';

class AzkarNightPage extends StatelessWidget {
  const AzkarNightPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Night Azkar'),
      ),
    );
  }
}
