import 'package:flutter/material.dart';

class MobileRegistration extends StatelessWidget {
  const MobileRegistration({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: const Text('mobile registration'),
      ),
    );
  }
}
