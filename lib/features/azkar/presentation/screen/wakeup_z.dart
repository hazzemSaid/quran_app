import 'package:flutter/material.dart';

class wakeupAzkarPage extends StatelessWidget {
  const wakeupAzkarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Wakeup Azkar'),
      ),
    );
  }
}
